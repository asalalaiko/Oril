CREATE TABLE "pair" (
                        "id" SERIAL PRIMARY KEY,
                        "curr1" varchar NOT NULL,
                        "curr2" varchar NOT NULL,
                        "lprice" varchar NOT NULL,
                        "created" timestamp NOT NULL
)
